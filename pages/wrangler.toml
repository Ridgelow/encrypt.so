# Static marketing site for encrypt.so.
# The API worker config is workers/wrangler.toml — do not deploy that from here.
name = "encrypt-so"
pages_build_output_dir = "./public"
compatibility_date = "2026-09-22"
